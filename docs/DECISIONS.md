# 架构决策

## ADR-001：本地无账户单页 MVP（已接受，2026-07-11）

采用浏览器内存计算和静态友好部署；无数据库、账号、追踪或外部 API。隐私和维护风险低，代价是刷新后进度消失。

## ADR-002：内容、评分、界面分层（已接受）

题目资料、纯评分函数和页面流程分别放置，降低内容修改破坏算法的风险。

## ADR-003：可解释规则而非机器学习（已接受）

使用八维加权与现实门槛；同输入有稳定结果。权重仍是待验证草案。

## ADR-004：Sites TypeScript/React 骨架（暂定）

用于类型、构建和托管一致性，无付费服务。原生 HTML 更轻但类型与扩展保障较弱；长期选择见 OQ-001。

## ADR-005：品牌、地区与内容范围（已接受，2026-07-11）

正式名称为 Pet Match，主要面向中国大陆简体中文用户；视觉采用可爱温柔的绿粉色圆角风格。第一版保留犬、猫、小宠、水族和“先体验”，结果给出明确代表品种，但必须同时呈现个体差异和动物福利提示。

## ADR-006：无持久化的结果分享（已接受，2026-07-11）

第一版不保存答题进度、不使用访问统计。分享功能只调用浏览器/手机系统分享，或复制结果文字和当前网页链接；不把答案编码进链接，也不发送到服务器。

## ADR-007：使用 GitHub Pages 临时托管（已接受，2026-07-11）

暂不购买域名，使用 GitHub Pages 提供的项目临时网址。保留现有 Sites 开发构建，同时增加 Next.js 纯静态导出与 GitHub Actions 发布流程。GitHub Pages 不运行服务器逻辑，因此未来加入账户、数据库或服务端 API 时必须重新评估托管方案。内容暂不做专业复核，不得作为正式专业建议推广。

## ADR-008：本地生成结果分享图片（已接受，2026-07-11）

使用浏览器 Canvas 生成 1080×1350 PNG，包含宠物类型、品种、原因和免责声明。移动设备支持时分享图片文件，否则自动保存；另提供明确的保存按钮。无新生产依赖，无答案上传。

## ADR-009：12 品种距离匹配（已接受，2026-07-11）

已由 ADR-011 取代。

## ADR-010：不接入统计服务（已接受，2026-07-11）

为坚持不追踪用户，网页不加载统计脚本或计数 API。项目所有者可在 GitHub 仓库 `Insights → Traffic` 查看最近 14 天的仓库浏览与独立访客近似数据；GitHub 官方将其定义为仓库流量，因此不能当作 Pet Match 页面点击的精确计数。若未来需要精确网站浏览量，必须重新确认外部统计服务和隐私披露。

## ADR-011：35 题、26 品种与十二维匹配（本地待验收，2026-07-11）

已由 ADR-012 取代。

## ADR-012：性格优先的 35 题与 90 品种库（本地待验收，2026-07-12）

题库保留 12 道必要现实条件题，其余 23 道用于性格和相处偏好。品种库扩展为犬 30、猫 30、小宠 15、水族 15，共 90 个明确品种；自动测试验证数量、唯一性和所有品种可达。安全门槛不再完全隐藏性格结果，而是在“先体验”结果下方展示小号参考。

## ADR-013：本地二维码生成依赖（已接受，2026-07-12）

引入 `qrcode@1.5.4` 作为生产依赖，唯一用途是把固定公开网址在浏览器本地生成二维码；引入 `@types/qrcode` 作为开发类型依赖。第三方二维码 API 会增加网络请求、可用性和隐私风险，因此不采用。依赖需随常规依赖审计维护。
