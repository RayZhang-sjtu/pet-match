# 测试

`npm run check` 依次执行格式、lint、类型、单元测试、Sites 生产构建和 GitHub Pages 静态构建；任何一步失败均视为未完成。

自动覆盖 30 题与唯一 ID、结构完整性、缺答和非法值、逐项现实硬门槛、固定结果与构建。结果类别完整可达性和浏览器交互自动测试尚未建立，列为上线前任务。人工需在手机与桌面各完成一次，检查前后导航、进度、答案保留、键盘焦点、结果和重新测试。上线前另验 HTTPS、安全响应头、刷新路径及无意的第三方请求。
